{
  "name": "purchase_decision_making",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "miniapp/wx"
  ],
  "scripts": {
    "test": "vitest run",
    "build": "npm run build --workspaces",
    "sync:snapshot": "node scripts/sync-snapshot.mjs"
  },
  "license": "CC-BY-NC-4.0",
  "devDependencies": {
    "jsqr": "^1.4.0",
    "playwright": "^1.62.1",
    "qrcode-generator": "^2.0.4"
  }
}
