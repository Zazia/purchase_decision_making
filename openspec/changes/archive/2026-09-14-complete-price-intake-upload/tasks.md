## 1. 保存上下文与上传数据

- [x] 1.1 为重算结果绑定不可变上传上下文，贯通保存原始版、修改版和本地回看；测试后续未重算编辑不混入已保存结果，旧缓存无来源只分享。
- [x] 1.2 构造分享上传 DTO，含 reportData、规范化 submittedPlans 和原始完整方案对照；测试 2700 改 2500、普通推荐零样本、排除/暂缓、预测、自定义、渠道及国补口径。
- [x] 1.3 为保存内容维护指纹、同意版本、submissionId/cloudId；测试相同内容重试复用，新内容重新确认，原始价与 editedBuyPrice 不被上传构造改写。

## 2. 单次确认与分享生成

- [x] 2.1 在现有「展示我的方案」生成流程加入一次统一确认并更新用途说明；页面验证默认开启也会确认、未开启不上传、取消继续本地保存。
- [x] 2.2 统一生成进度和重试反馈；测试上传失败保留本地结果，相同提交重试不再确认，云写成功但二维码/渲染失败不重复保存。
- [x] 2.3 退出独立 price-intake 提示/调用及旧偏好依赖，移除直接导出长图内绕过确认的 save；检索所有 save 调用并页面验证编辑、重算、直接导出零新增记录，长图仍可导出。

## 3. 扩展已有云函数与集合记录

- [x] 3.1 为 share-result/save 实现 v2 白名单校验、可信身份/时间、同意版本与完整快照落库；测试非法字段、超限、缺身份均零部分写入，普通推荐空 submittedPlans 合法。
- [x] 3.2 实现不超过 32 字符的幂等记录 ID 与原子写入；测试并发/超时重试仅一条、相同 ID 不同内容冲突、返回 ID 能生成小程序码。
- [x] 3.3 保持旧 save/get/qrcode 兼容，公开 get 仅返回分享字段；测试旧参数记录可读且不补造分析授权，v2 分析元数据不泄露。
- [x] 3.4 调整 v2 过期读取为只失效不删除，保留旧记录兼容策略；测试 30 天后分享不可访问但管理端仍可读取分析记录。

## 4. 直接消费 shared_results

- [x] 4.1 管线切换默认来源，按版本、同意、用户价格与测试标记筛选，正确处理毫秒时间；测试旧参数/普通分享/测试各自跳过，缺集合报错、空库正常。
- [x] 4.2 实现单条分享内持有期和副本去重，按配置/渠道/国补分组；测试三种持有期只增加一条价格观测，不同配置和价格口径不混组。
- [x] 4.3 升级台账版本和来源标记，提供独立台账路径重建及按共享 ID 只读诊断；测试旧非空台账不覆盖、重复运行不增样本、诊断不改正式台账。
- [x] 4.4 同步改造报告的分组字段解析、渠道/国补展示、constants 现值匹配和原始推荐对照；测试不同配置不串价、不可比口径明确待核对、来源/跳过数可见，旧报告保留且新报告不泄露身份。
- [x] 4.5 保持 pnpm intake:report 与既有凭证/环境入口，补诊断和台账参数说明；通过原命令验证读取新来源、生成报告/台账/缓存，确认产物路径与 gitignore 约定兼容。

## 5. 部署与端到端验收

- [x] 5.1 核验实际环境、share-result 和 shared_results 权限，在 verification.md 记录状态；确认无需第三集合且客户端不能直接读取整条分析记录或写库。
- [x] 5.2 部署兼容旧客户端的 share-result，复核函数版本和权限；验证旧分享可用、新保存返回可生成小程序码的 ID。
- [x] 5.3 用真实页面保存修改结果、确认一次上传 isTest 数据，按 ID 复核用户价 2500、对照价 2700、同意版本及一条写入；离线从同一 ID 诊断且正式样本不受影响。
- [x] 5.4 回归不勾选、取消、普通结果、修改版、上传失败、二维码失败、重算/导出和过期读取；验证 constants 不变，记录开发者工具验证、云部署与客户端发布各自状态。
- [x] 5.5 回归已完成的数据维护工具：运行 constants:lint、constants:release 干跑及既有失败中止/回读核验测试，确认发布编排兼容；此验收不发布真实价格，记录人工审核后接回既有发布命令的流程。

## 6. 文档与归档

- [x] 6.1 更新 README 和数据维护文档，说明两集合、唯一确认入口、直接消费、历史跳过、保留/清理与台账迁移；核对说明和已验证行为一致。
- [x] 6.2 调试出现已解决的新卡点时回填 wx-miniprogram-autotest 技能；无新卡点则记录不适用，验收证据不含身份或凭证。
- [x] 6.3 同步两个 delta，更新主规格 Purpose 中的数据源描述、修复 Requirements 标题并验证；全部实施验收完成后归档且无 apply 进度文件残留。
