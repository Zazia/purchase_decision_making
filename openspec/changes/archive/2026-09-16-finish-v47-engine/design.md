# 设计

- 沿用已落盘的 ProMax/Duo 显式芯片映射，通过候选生成接口验证芯片与性能，保留标准版映射。
- 测试克隆 constants，固定分析日期、iPhone Pro/ProMax 发布月和置信度；真实数据的滚动行为另行验证，不修改生产数据迎合测试。
- 类型 C 残值测试使用明确的 iPhone_Pro 子品类，避免父品类按最高置信度选择其他发布计划。
- 构建后同步 vendor 和快照，dry-run 后发布云端并读回比对。引擎 vendor 变更仍需小程序后续发版才能到达线上客户端。
