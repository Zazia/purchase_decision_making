## 1. 建立审计基线与风险控制

- [x] 1.1 记录当前本地 constants、miniapp 快照和云端 constants 的版本/哈希，并把只读核验结果写入 `scripts/debug/`，作为迁移与回滚基线
- [x] 1.2 为现有残值报告生成器增加冻结测试，复现“70 个点中 20 个同品类在售新品分母、50 个说明文本分母”和 Mac mini v4.8 MAE 口径错误
- [x] 1.3 在维护文档中标记现有残值报告偏差/MAE 不得继续用于曲线校准，直至新管线通过

## 2. 实现结构化首发价与观测模型

- [x] 2.1 在 constants schema 中新增首发价目录，定义型号、配置、地区、发布日期、首发官方价、来源和验证状态字段
- [x] 2.2 在 constants schema 中新增二手价格观测结构，定义价格类型、成色、样本量、置信度、事件状态、首发价引用和校准资格
- [x] 2.3 实现观测归一化脚本，分别计算曲线观测保值率、替代价值率和个体投资保值率，并将派生 JSON/CSV 写入 `scripts/debug/`
- [x] 2.4 添加单元测试，覆盖同配置精确匹配、CTO 首发价缺失、跨地区/币种拒绝、补贴实付与官方首发价分离、备注文本不得作为分母

## 3. 加固 constants lint 与发布门禁

- [x] 3.1 扩展 constants lint，校验首发价/观测引用完整性，并拒绝当前新品价、后续调价、实际购入价或说明文本成为校准分母
- [x] 3.2 实现曲线变更检测与校准 manifest 校验：输入观测、排除原因、拟合参数、指标、脚本版本和目标 constants 版本必须齐全
- [x] 3.3 将曲线审计门禁接入维护流水线，保证失败发生在本地快照同步和任何云端写入之前
- [x] 3.4 添加“仅市场快照更新”快速路径测试，确认未改曲线时无需运行完整交叉验证但仍执行结构引用校验

## 4. 迁移现有数据并审计全部品类

- [x] 4.1 将当前报告中的 70 个二手点迁移为结构化观测；保留原始价格与来源，无法核验首发价的记录标记为不可校准，不猜测、不删除
- [x] 4.2 优先从苹果官方发布资料补齐 Mac mini M1/M2/M4 的基础配置及现有观测涉及配置的国行首发价
- [x] 4.3 补齐 iPhone、MacBook、iPad 已有报告点涉及型号/配置的首发价；高配/CTO 无官方证据时保持不可校准
- [x] 4.4 实现全品类审计脚本，覆盖 constants 中全部 18 个曲线品类，并输出 `scripts/debug/residual-curve-audit.json` 与可读摘要
- [x] 4.5 为每个品类赋予 `invalid_calibration_applied`、`invalid_evaluation_only`、`provenance_missing` 或 `verified` 状态，人工复核 Mac mini 与其他已有观测品类的分类证据
- [x] 4.6 添加审计覆盖测试：18 个品类无遗漏、状态合法、证据与建议动作非空，且审计过程不得修改 constants

## 5. 撤销 Mac mini v4.8 错误校准

- [x] 5.1 将 Mac mini 18–48 月节点从 v4.8 恢复至 v4.5 临时基线，移除“23 月必须接近 55%”和错误 MAE 断言，并标记 provisional/unverified
- [x] 5.2 更新 constants 元数据与修订说明，明确 v4.8 失效原因、临时基线局限和正式重拟合前置条件
- [x] 5.3 运行 constants lint、相关测试与全量引擎测试，确认买入价锚定残值不变量和其他品类结果未发生意外变化
- [x] 5.4 重新生成兼容口径报告、同步小程序本地快照，执行云发布 dry-run；通过后正式发布并核验云端版本、payload 与哈希

## 6. 实现可复现的曲线拟合器

- [x] 6.1 实现标准节点上的单调约束加权拟合，固定 `R(0)=100`、遵守 floor，并加入相邻斜率二阶差分正则
- [x] 6.2 实现样本权重与资格规则：成交/实付、精确首发价、多时点稳定性加权；挂单、回收报价和低置信样本降权；不合格样本排除并记录原因
- [x] 6.3 实现配置与事件分层：无配对基础款的高配仅作验证，发布窗口/官方调价/缺货泡沫默认不进入基线拟合
- [x] 6.4 实现留一代交叉验证；代际不足的品类回退留一时点验证，并强制 provisional 状态与至少 90 天覆盖门槛
- [x] 6.5 生成校准 manifest，包含训练/验证加权 MAE、MAPE、样本覆盖、节点变化、正则参数、输入与排除观测 ID
- [x] 6.6 添加合成数据测试，覆盖单调性、异常代际低点、配置混合、事件样本排除、正则参数选择和错误分母即使 MAE 更低也必须失败

## 7. 正确重拟合 Mac mini 并验证其他品类

- [x] 7.1 用结构化 M1/M2/M4 合格观测运行 Mac mini 拟合；若未满足跨代际/年龄带门槛则保留临时基线并输出缺口清单，不强行发布新节点
- [x] 7.2 若 Mac mini 通过门槛，更新正式节点、floor/half-life 评估、校准 manifest 与节点回归测试，并比较对各买入机龄/持有期残值比率的影响
- [x] 7.3 对 iPhone、MacBook、iPad 已有观测子品类运行只读验证，区分可重拟合、仅报告修复和数据不足三类结果
- [x] 7.4 对其余无足够观测品类保留现有节点并补充来源/未验证状态，不因全品类审计自动改值

## 8. 修复报告与市场快照审计语义

- [x] 8.1 修改 `scripts/analyze-market-snapshot.mjs`，删除通过说明文本或同品类首个新品价推断曲线分母的逻辑，改用结构化首发价引用
- [x] 8.2 修改 `scripts/visualize-residual-curves.mjs`，理论曲线只与曲线观测保值率计算偏差/MAE/MAPE；替代价值率与个体投资保值率使用独立字段和图层
- [x] 8.3 在报告中展示品类审计状态、训练/验证样本数、排除原因和校准置信度，禁止把挂单价描述为成交价
- [x] 8.4 重新生成 `scripts/debug/` payload 和自包含 HTML，校验不再出现“口径存在系统偏差但仍给出拟合改善”的矛盾说明

## 9. 同步方法论、规格与分发

- [x] 9.1 更新 `SKILL.md` 的曲线定义、年度/例外更新门槛和三类价格比率；更新 `reference/market-sampling.md` 与 `reference/pricing-scenarios.md`
- [x] 9.2 更新技能 CHANGELOG，并同步 OpenSpec 主规格：新增 `residual-price-observation-schema`，更新 `residual-curve-calibration` 与 `constants-maintenance-tooling`
- [x] 9.3 按 AGENTS.md 约定把 SKILL、CHANGELOG、reference 与脚本单向同步至公开 `apple-value-analysis` 仓库，验证 diff 不包含 constants 或本地报告
- [x] 9.4 运行所有相关测试、constants lint、全品类审计、报告校验、本地快照同步、云端发布 dry-run/正式发布与云端哈希核验
- [x] 9.5 提交主仓库变更并推送 GitHub/Gitee 主分支；提交并双远程推送公开技能仓库，记录两个仓库的提交与发布结果

发布记录（2026-09-25）：

- 主仓库：`237a312`，已推送 `origin/main` 与 `gitee/main`
- 公开技能仓库：`d7fca28`，已推送 `origin/master` 与 `gitee/master`
- 云端：`constants/latest` version=`2026-09-25`，sha256=`96e8e47eaab28bb34e284ce9493434fbc02bf047e2bbf1171fc7896441d077c4`，正式发布与回读核验通过
